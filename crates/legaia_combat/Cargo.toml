[package]
name = "legaia_combat"
version = "0.1.0"
edition = "2021"

[dependencies]
bevy = "0.14"
rhai = { version = "1.19", features = ["sync"] }
serde = { version = "1.0", features = ["derive"] }
anyhow = "1.0"
rand = "0.8"

[dev-dependencies]
