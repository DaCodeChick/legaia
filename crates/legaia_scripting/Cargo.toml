[package]
name = "legaia_scripting"
version = "0.1.0"
edition = "2021"

[dependencies]
bevy = "0.14"
mlua = { version = "0.10", features = ["lua54", "send", "serialize"] }
serde = { version = "1.0", features = ["derive"] }
anyhow = "1.0"
rand = "0.8"

[dev-dependencies]
